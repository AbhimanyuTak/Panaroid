Requires a Server:

Creating Localhost using Python:
Windows:
1) Install python 2.7
2) Set Path Variable
3) Run python -m SimpleHTTPServer from cmd
4) Go to the http://localhost:8000
5) Go to the downloaded directory from browser
6) Run index.html(automatic in most cases)

Linux:
1) Install python 2.7
2) Run python -m SimpleHTTPServer from terminal
3) Go to the http://localhost:8000
4) Go to the downloaded directory from browser
5) Run index.html(automatic in most cases)
